---
title: "2026数字中国pwn-先知社区"
source: "https://xz.aliyun.com/news/91966"
author:
published:
created: 2026-06-10
description: "先知社区是一个安全技术社区，旨在为安全技术研究人员提供一个自由、开放、平等的交流平台。"
tags:
  - "clippings"
---
---
title: "2026数字中国pwn-先知社区"
source: "https://xz.aliyun.com/news/91966"
author: ""
published: ""
created: "2026-06-10T21:10:55+08:00"
description: "先知社区是一个安全技术社区，旨在为安全技术研究人员提供一个自由、开放、平等的交流平台。"
tags: [clippings]
---

# 2026数字中国pwn-先知社区

2026数字中国pwn

当时我们是在飞机上，没有来得及打这个比赛，赛后复习一下这个题目

PWN1：keep\_stack

让ai还原了一下符号，因为不还原的话实在是太难看了

这里补充一下格式化的基础知识

• %zu 是 C 里用于 size\_t 的格式说明符，常见于 printf / scanf 这类函数。

●

z：长度修饰符，表示参数类型是 size\_t

●

u：按无符号十进制输出/读取

这里的ctx是在栈上申请的，这个并不在堆上

int prompt\_and\_echo\_message()

{

message\_ctx\_t ctx;

unsigned int unused\_seed;

unused\_seed = 0;

puts("length: ");

\_\_isoc99\_scanf("%zu", &ctx.max\_units);

unused\_seed = derive\_seed\_from\_length(ctx.max\_units);

memset(&ctx, 0, 0x80u);

puts("What do you want to say? ");

reset\_write\_index\_and\_xor\_mode1(&ctx);

xor\_mode3(&ctx);

read\_sparse\_message(&ctx);

return printf("input: %s\\n", ctx.text);

}

这个限制了我们的长度必须是0x10以内，这里是无符号类型的，并不存在类型转化的溢出漏洞的

还有就是这个函数的功能好像是派生出来一个seed，并不是后面的读写所依据的

depth\_cap = 8;

if ( input\_length % 0xA + 5 <= 8 )

depth\_cap = input\_length % 0xA + 5;

depth = depth\_cap;

完整的函数如下，这个是混淆，这个函数的返回值没有直接用到

unsigned int \_\_fastcall derive\_seed\_from\_length(unsigned int input\_length)

{

unsigned int depth\_cap;

unsigned int transformed\_value;

unsigned int depth;

int round\_idx;

unsigned int seed;

depth\_cap = 8;

if ( input\_length % 0xA + 5 <= 8 )

depth\_cap = input\_length % 0xA + 5;

depth = depth\_cap;

if ( (input\_length & 1)!= 0 )

{

transformed\_value = apply\_transform\_rounds(input\_length, depth\_cap);

seed = double\_recurse\_then\_mul3(transformed\_value, depth);

}

else

{

seed = compute\_even\_seed(input\_length, depth\_cap);

}

for ( round\_idx = 0; round\_idx <= 4; ++round\_idx )

seed = g\_transform\_ops\[(round\_idx + seed) % 0xA\](seed);

return seed ^ 0x1234ABC;

}

后面还有一个函数会将我们的输入和1做异或

之后还有异或

这里我们进行溢出写的部分，前面我们看到这个ctx是在栈上申请的，这里的话是存在ctx->text这个 成员越界的

我们主要看的这一个地方

我们选择步长是4的时候，在0x20的就已经越界了，但是4的话会比较稀疏，这里的话我们选择2，这样的话覆写会更加的完全

这里我们是一开始4字节跨度不能改的，所以说我们第一次的时候选择修改返回地址重新返回，利用第二次的时候完成修改这个任务。

这个类型的题目我们做了很多变了，在溢出中需要注意 维护好这个结构体

看一下 调试的时候 栈帧的空间

这里正常写满的情况，我们想要溢出写的话，我们计算一下溢出

一个是0x20这里是4次，还有就是0x10，这里又是4次，之后就是返回地址，所以说此理论上参数是0x29次

但是这个是不断更新，我们这里溢出写的时候需要额外计算，我们把max\_units改成0x24即可，在0x23的时候把write\_index改成0x29即可

我们通过慢调试，发现了一个问题，这个虽说是导致我们退出的原因

这里的跳转不能直接性的这样去改，可以维持正常的逻辑去改

在之后再次跳转就行了，选择一段汇编指令调整一下栈就行了

之后2字节的跳转也是同理，我们可以定义函数进行封装,这里主要就是需要断点在结构体修改的位置，花个十几分钟反复走一下就行

![](https://xzfile.aliyuncs.com/media/upload/picture/20260414165125-1ea0536b-37df-1.png)

PWN2：hkobj（环境启动+8字节任意地址写）

这个题目没有 找到泄漏点，这个题目的ioctl提供的功能很有限，kmalloc提供的种类很少

这里没有什么uaf，直接就是白给的任意地址write，在满足的一定的条件下

我想找一个结构体重合，但是到现在还是一直没有找到，找到了也没有什么用，因为没有泄露点；还有就是开启独立的cache，cross cache很难实现，这里alloc限制了只能申请三个，还有就是没有uaf的漏洞，也就没法进行重用，这里锁死了这个利用

现在题目提供ioctl直接给功能，以往都是通过ioctl提供的chunk来进行劫持结构体的，这个是真不会

这个是一个内核的题目，这个题目的是aarch64架构的，我们的x86-64架构的并不能正常的运行，这个是这这个题目最大的不同之处

这里是hex-rays这个插件出问题了，好像是处理不了这么复杂的情况

然后的话，题目给了我们 dockerfille文件，我们可以 利用dockerfile直接启动

具体的启动的命令如下

这里需要们 启动一下代理，要不下载不了

之后开启另一个终端传进去就行，这样就可以跳过打包的麻烦的一步了

这里是因为题目的run.sh是对于exp.bin这个文件进行特殊的处理的

这个过程会很卡，这里我们本地的猜测的时候直进docker容器进行模拟

当出现命令行提示符的时候，说明我们的文件已经传完了，我们可以直接进入docker，减少中转进行 加速，进入的方法如下

之后的话还需要去手动启动一下start.sh

运行效果如下，这个是具体的流程 ，并没有打通，我们需要泄露内核基址

我们来重新分析一下这个题目

这里的alloc和edit对对象的处理比较特殊，这里着重来讲解一下是如何实现的任意地址写

我们先看do\_alloc这个函数，主要是实现了8字节的写

这里是限定特定了cache

对应的结构体如下

然后再看 do\_write这这个函数，主要是实现了8字节写

之后再看ioctl这个函数，首先是拷贝了基础的结构体进行选择

之后再进行调用

漏洞的话主要是下面这个，这里每次alloc之后都是向后平移0x10字节的

这里是最奇怪的地方，这里的对象之间是彼此影响的

这里alloc的第三个时候，把【5】这个位置写上我们target的地址

然后的话还有这里，这个是漏洞的地方，这里if的片段，是额外的向下越界写8字节，之后就能覆写user\_payload.buf这个全局变量，进而实现任意8字节写

这个的注意维护一下buf，alloc是用来分配对象，write是用来往对象里面写

这个判断在漏洞的比较的相当于

我们控制输入的数值相等的话就能调用edit，进而实现任意地址写8字节

题目是开启了kaslr的，这里我们没有想到泄露地址的方法，这个题目虽然相当于白送了一个msg\_msg的任意地址写，但是没有送任意地址读